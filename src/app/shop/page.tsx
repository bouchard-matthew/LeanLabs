"use client";

import { Typography } from "@mui/material";
import { Container } from "../../Design";

const page = () => {
  return (
    <Container>
      <Typography>All Products</Typography>
    </Container>
  );
};

export default page;
